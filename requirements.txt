# AlphaGenome + State + Tahoe-100M Comprehensive Pipeline Dependencies

# Core AlphaGenome dependencies (existing)
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.4.0
scipy>=1.7.0
protobuf>=3.20.0
grpcio>=1.46.0
grpcio-status>=1.46.0
absl-py>=1.0.0

# Single-cell analysis and transcriptomics
anndata>=0.8.0
scanpy>=1.9.0
h5py>=3.7.0

# Hugging Face datasets for Tahoe-100M integration
datasets>=2.0.0
pyarrow>=10.0.0
fsspec>=2022.1.0

# Machine learning and data processing
scikit-learn>=1.1.0
torch>=1.12.0  # For State model compatibility
transformers>=4.20.0  # For Hugging Face integration

# Data validation and gene annotation
requests>=2.28.0
urllib3>=1.26.0

# Visualization enhancements
seaborn>=0.11.0
plotly>=5.10.0

# Performance and caching
joblib>=1.1.0
tqdm>=4.64.0

# Development and testing
pytest>=7.0.0
pytest-cov>=3.0.0

# Optional: Arc Institute State model dependencies
# (These should be installed separately via uv tool install arc-state)
# arc-state  # Install with: uv tool install arc-state

# File format support
openpyxl>=3.0.0  # Excel export support
xlsxwriter>=3.0.0  # Excel formatting

# Logging and configuration
pyyaml>=6.0
python-dotenv>=0.20.0

# Memory management for large datasets
psutil>=5.9.0
memory-profiler>=0.60.0

# Network and API handling
httpx>=0.23.0
aiohttp>=3.8.0

# Type hints and code quality
typing-extensions>=4.3.0
jaxtyping>=0.2.0  # For array type hints (if available)

# Statistical analysis
statsmodels>=0.13.0
pingouin>=0.5.0  # Statistical tests for genomics

# Parallel processing
multiprocessing-logging>=0.3.0
concurrent-futures>=3.1.1

# Data serialization
pickle5>=0.0.11  # For Python < 3.8 compatibility
cloudpickle>=2.0.0

# GPU support (optional)
# cupy>=11.0.0  # Uncomment for GPU acceleration
# rapids-singlecell>=0.9.0  # Uncomment for GPU-accelerated single-cell analysis